# Optional overrides — if unset, the frontend auto-detects the backend URL from
# window.location (works for both localhost and forwarded Codespaces/devcontainer URLs).
# VITE_API_URL=http://localhost:3000
# VITE_WS_URL=ws://localhost:3000
